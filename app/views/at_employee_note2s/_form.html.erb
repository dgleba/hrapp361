<%= simple_form_for(@at_employee_note2) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name_if_not %>
    <%= f.association :employee %>
    <%= f.input :issue %>
    <%= f.input :date_if_not_today %>
    <%= f.association :user %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, class: 'btn btn-success'%>
  </div>
<% end %>
