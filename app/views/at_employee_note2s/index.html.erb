
<row class="row is-table-row" >

  <div class="col-sm-1 ">
    <%= link_to 'Create', new_at_employee_note2_path , class: "btn btn-success"%>
  </div>

  <div class="col-sm-2 ">
    <h4>Listing At Employee Note2s</h4>
  </div>

  <div class="col-sm-3">
    <%= search_form_for @q, class: 'search-form' do |f| %>
      <div class="input-group">
        <%= f.search_field :name_if_not_cont, class: 'form-control' %>
        <span class="input-group-btn">
          <button class="btn btn-primary">Search <i class="fa fa-search"></i>
          </button>
        </span>
      </div>
    <% end %>
  </div>

  <div class="col-sm-4 ">
    <%= paginate @at_employee_note2s %>
  </div>
  
  <div class="col-sm-2 ">
    <%= page_entries_info @at_employee_note2s, :entry_name => 'item' %>
  </div>
  
</row>


<table class="table table-striped">
  <thead>
    <tr>
      <th colspan="1"></th>
            <th>Name if not</th>
      <th>Employee</th>
      <th>Issue</th>
      <th>Date if not today</th>
      <th>User</th>
    </tr>
  </thead>

  <tbody>
    <% @at_employee_note2s.each do |at_employee_note2| %>
      <tr>
        <td>
          <div class="btn-group">
          <%= link_to 'Show', at_employee_note2, class: "btn btn-info", :style=>'color:#FFFFFF;' %>
          <%= link_to 'Edit', edit_at_employee_note2_path(at_employee_note2), class: "btn btn-primary", :style=>'color:#FFFFFF;' %>
          <%= link_to 'Destroy', at_employee_note2, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-warning", :style=>'color:#FFFFFF;' %>
          </div>
        </td>
        
        <td><%= at_employee_note2.name_if_not %></td>
        <td><%= at_employee_note2.employee %></td>
        <td><%= at_employee_note2.issue %></td>
        <td><%= at_employee_note2.date_if_not_today %></td>
        <td><%= at_employee_note2.user %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
